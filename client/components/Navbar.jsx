import { Typography } from "@mui/material";
import React from "react";

const NavBar = () => {
    return(
        <div>
            <Typography>
            This is a placeholder for the navigation bar.
            </Typography>
        </div>
    );
};

export default NavBar;